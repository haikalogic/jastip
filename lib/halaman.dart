import 'package:flutter/material.dart';
class halaman extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("hai", style: TextStyle(fontSize: 30.0),),
      ),
    );
  }
}